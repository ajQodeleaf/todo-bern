'use client';
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    colors: {
        primary: "#ff0000",
        secondary: "#00ff00",
    }
});

export default theme;